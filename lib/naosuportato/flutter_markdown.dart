import 'package:flutter/material.dart';

class Markdown extends StatelessWidget {
  final dynamic data;

  const Markdown({Key key, this.data}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Markdown Não Suportado");
  }
}


class MarkdownBody extends StatelessWidget {
  final dynamic data;

  const MarkdownBody({Key key, this.data}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Markdown Não Suportado");
  }
}
